# Copy this file to ".env.local" and paste your real Anthropic API key.
# Get one at https://platform.claude.com — it starts with "sk-ant-".
# .env.local is git-ignored, so your key stays on your machine.
ANTHROPIC_API_KEY=your_key_here
